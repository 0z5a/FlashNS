# CPU mathematical validation only. Prefer an existing isolated environment.
# These are the actually tested package versions, not a GPU-host upgrade plan.
numpy==2.3.5
scipy==1.17.0
mpmath==1.3.0
# Use the matching CPU distribution from your environment/package index.
torch==2.10.0
